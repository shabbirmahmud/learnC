#include <stdio.h>
// What is the value of 0??1722765 % 1024 in C? The ? digits are unknown, but they are valid digits for the number.

int main()
{
    int num1 = 0001722765, num2, result;
    for (int i = num1; num2 <= 0771722765; i++)
    {
    printf("num1 to num2 modulus is %d\n", result = num1 % 1024);
    }
    return 0;
}
